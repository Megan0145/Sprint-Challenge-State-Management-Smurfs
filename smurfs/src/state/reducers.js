import * as types from "./actionTypes";

const initState = {
    smurfs: []
}

export function smurfReducer(state = initState, action){
    switch (action.type){
        default: 
        return state;
    }
}